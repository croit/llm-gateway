/target
**/*.rs.bk
Cargo.lock.bak

# Claude Code session state
.claude/

# Local config and secrets
*.local.toml
.env
.env.*
!.env.example

# Real deployment secrets/config (only the *.example.* templates are committed)
deploy/*.env
deploy/gateway.toml

# Editor/OS
.idea
.vscode
*.swp
.DS_Store

# mise installs its tools project-locally if asked; tracked separately
.mise.toml.local

# Dev-mode SQLite (created by `mise run dev` when no $GATEWAY_CONFIG is set)
gateway.sqlite*
/.playwright-cli/

# Local dev RAG storage: the `[rag] data_dir` tree (default ./data/rag).
# Per-collection sqlite + usearch vector indexes + git clone caches —
# all regenerable and potentially many GB. Never commit.
/data/

# JS bundle source maps — emitted alongside crates/session-core/assets/*.js
# by the esbuild step. The bundles themselves are committed (matching the
# CSS bundle convention), but the maps are dev-only and large.
crates/session-core/assets/*.js.map

# GeoIP database 
IP2LOCATION-LITE-DB11.BIN

